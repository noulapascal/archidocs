{% extends 'base.html.twig' %}

{% block title %}CompanyDivision{% endblock %}

{% block body %}
    <h1>{% trans %}CompanyDivision{% endtrans %}<h1>

    <table class="table">
        <tbody>
            <tr>
                <th>{% trans %}Id{% endtrans %}</th>
                <td>{{ company_division.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('company_division_index') }}">back to list</a>

    <a href="{{ path('company_division_edit', {'id': company_division.id}) }}">{% trans %}edit{% endtrans %}</a>

    {{ include('company_division/_delete_form.html.twig') }}
{% endblock %}
