<form method="post" action="{{ path('company_division_delete', {'id': company_division.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company_division.id) }}">
    <button class="btn">Delete</button>
</form>
